{
  "name": "omarchy-nostr-signer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Nostr key custody daemon: holds an encrypted nsec, signs on request over a Unix socket for first-party Omarchy plugins, and answers NIP-46 remote-signing requests over relays.",
  "bin": {
    "omarchy-nostr-signer-ctl": "./bin/ctl.mjs"
  },
  "scripts": {
    "start": "node src/daemon.mjs"
  },
  "dependencies": {
    "@nostr-dev-kit/ndk": "^3.0.3",
    "nostr-tools": "^2.25.0",
    "ws": "^8.21.3"
  }
}
